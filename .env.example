# Backend
DATABASE_URL=postgresql+psycopg://user:password@host/dbname
CORS_ORIGINS=["http://localhost:5173","https://prunape.pages.dev"]
ENV=dev
LOG_LEVEL=INFO

# Clerk Auth (dejar vacío para deshabilitar auth en dev)
CLERK_JWT_ISSUER=
CLERK_JWKS_URL=

# Fly.io (solo en prod, via fly secrets set)
# fly secrets set DATABASE_URL=...
# fly secrets set CLERK_JWT_ISSUER=...
# fly secrets set CLERK_JWKS_URL=...
